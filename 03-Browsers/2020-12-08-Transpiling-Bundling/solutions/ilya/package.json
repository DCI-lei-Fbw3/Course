{
  "name": "ilya",
  "version": "1.0.0",
  "description": "**Instructions**:\r * Look through the starting files provided in this repository. You will find the logic of the calculator app in `percentage.js` and `aspect-ratio.js`. \r * Import the files to `index.js` and add the functionality to the UI in the `index.js` file.\r * Do not forget to add the `index.js` file at the end of the `index.html` file.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel modules_es6 -d modules_es5"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.12.8",
    "@babel/core": "^7.12.9",
    "@babel/preset-env": "^7.12.7"
  }
}
