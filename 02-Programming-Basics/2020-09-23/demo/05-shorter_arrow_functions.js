// const <function name> = (<parameters>) => <return value>

const sum = (a, b) => {
	return a + b;
};

console.log(sum(1, 1));

// shorter version
const add = (a, b) => a + b;

console.log(add(2, 4));
