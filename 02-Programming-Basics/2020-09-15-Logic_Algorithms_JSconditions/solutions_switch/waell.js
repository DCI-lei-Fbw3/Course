let determiner = 5;
if ( determiner < 0 ) {
    let x = "Less than 0"
} else ( determiner >= 0 )
    let x = "Greater or equal to 0";
    console.log(x);
if ( determiner >= 0 )  {
    let updater = "Greater or equal to 0"
} else ( determiner < 0 )
    let updater = "Less than 0";
    console.log(updater);

    let fruit = prompt("kind of fruit")
if (fruit == "apples") console.log("Apples are ok.");
else if (fruit == "bananas" ) console.log("Bananas are good before and after a football match.");
else if (fruit == "oranges" ) console.log("I can eat oranges all day!");
else if (fruit == "mangoes" ) console.log("Did someone say mangoes? Where?");
else console.log("this fruit is not available");