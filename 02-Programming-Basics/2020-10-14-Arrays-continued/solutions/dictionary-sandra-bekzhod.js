
function dictionary(initial, array) {
	let dictionary = array.filter(element => element.startsWith(initial)); 
	return dictionary;
}

function dictionary(initial, array) {
    return array.filter(element => element.startsWith(initial));
}
