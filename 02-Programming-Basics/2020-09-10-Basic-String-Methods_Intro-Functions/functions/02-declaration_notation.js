
// function FUNCTION_NAME(PARAMETER, ...) {
//     FUNCTION_BODY;
// }
//
// FUNCTION_NAME(PARAMETER, ...);

// NOTE: hoisting is possible => better readability

result = pow(2, 4);
console.log(result);

function pow(x, y) {
    return x ** y;
}
